%table
  %tr.text.left.first.h1.fallback-text
    %td= "Hi, #{@resource.firstname}"
  %tr.text.left.fallback-text
    %td Your account has been locked due to an excessive number of unsuccessful sign in attempts.
  %tr.text.left.fallback-text
    %td
      Please,
      = link_to 'click here to unlock your account.', unlock_url(@resource, unlock_token: @token)
      Or copy the following url into your browser:
      = unlock_url(@resource, unlock_token: @token)
  %tr.text.right.fallback-text
    %td - Fixus
